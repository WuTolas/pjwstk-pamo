<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pamo.lab4">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.lab4">
        <activity
            android:name=".activity.SplashActivity"
            android:theme="@style/Theme.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:label="Strona główna" />
        <activity
            android:name=".activity.BmiActivity"
            android:label="Kalkulator BMI" />
        <activity
            android:name=".activity.BmrActivity"
            android:label="Kalkulator kalorii" />
        <activity
            android:name=".activity.ChartActivity"
            android:label="Koronawirus Polska" />
        <activity
            android:name=".activity.QuizActivity"
            android:label="Quiz dietetyczny"/>
    </application>

</manifest>